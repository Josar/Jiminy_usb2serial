# Jiminy_usb2serial

This is the usb to serial bridge used for the Jiminy boards loaded onto the atmega16u2.
It is basically the serial to usb example from LUFA ASF with small edits for the board and reset pin behavior.
